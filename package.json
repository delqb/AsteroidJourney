{
  "type": "module",
  "dependencies": {
    "fluidengine": "file:../FluidEngine/fluidengine-0.0.0.tgz"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vite-tsconfig-paths": "^5.1.4"
  },
  "scripts": {
    "clean": "rmdir /s /q .\\dist",
    "fluidreload": "npm uninstall fluidengine --no-save && npm install fluidengine",
    "build": "npm run clean && fluidreload && tsc",
    "prepare": "npm run build"
  }
}